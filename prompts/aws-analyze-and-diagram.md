Analyze the relationships between the following AWS resources in account region **${1}** and perform these tasks:

1. Query AWS to gather details for each resource listed below.
2. Create an architecture diagram PNG at **${2}**.
3. Create a `relationship-analysis.md` at **${3}** analyzing the relationships between all resources.
4. Create a `testflow.md` at **${3}** with a step-by-step CLI test guide for the full flow (use `--no-cli-pager` where possible and `AWS_PAGER=""` for `logs get-log-events`).

## Resources

${4}
